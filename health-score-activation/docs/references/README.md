# References

**SQL query references** – Reusable patterns (e.g. counting-feature metrics) to copy when building activation queries.

**activation_health_score.yml** – Structured spec for the score: grain, scale (0–100), input categories, thresholds, and path to the final mart. For documentation only; no tool reads it.
